import styled from 'styled-components'
import { color, layout, typography, space, textStyle } from 'styled-system'

export const Text = styled.p`
  ${space}
  ${typography}
  ${textStyle}
  ${color}
  ${layout}
`
